// 配置跨域

module.exports = {
	devServer: {
		// port: '8080',
		// disableHostCheck: true,
		// proxy: {
		// 	'/api': {
		// 		target: 'http://localhost:8889',
		// 		changeOrigin: true,
		// 		pathRewrite: {
		// 			'^/api': ''
		// 		}
		// 	}
		// },
		// before: require('@/mock/index.js')
	}
}
